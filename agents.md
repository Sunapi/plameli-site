# agents.md

Global rule: always think in English and communicate with the user in Ukrainian.
Global rule: always read and follow design-system.md before generating or modifying layout or UI code.
Global rule: for layout, spacing, grid, container, typography, buttons, and cards, design-system.md is the authoritative specification unless the user explicitly overrides it.
Global rule: always read and follow design-system.md before generating or modifying layout or UI code.
Global rule: for layout, spacing, grid, container, typography, buttons, and cards, design-system.md is the authoritative specification unless the user explicitly overrides it.
