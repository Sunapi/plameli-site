---
interface Props {
  as?: string;
  class?: string;
}

const { as: Tag = "div", class: className = "" } = Astro.props;
---

<Tag
  class:list={[
    "rounded-xl border border-[var(--color-border)] bg-white p-8 shadow-sm",
    className,
  ]}
>
  <slot />
</Tag>
