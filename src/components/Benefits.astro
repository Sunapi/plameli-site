---
import Container from "./ui/Container.astro";
import Section from "./ui/Section.astro";
import Grid from "./ui/Grid.astro";
import SectionHeading from "./ui/SectionHeading.astro";
import { siteData, type Locale } from "../i18n/site";

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const data = siteData[locale].benefits;
---

<Section size="base">
  <Container>
    <SectionHeading class="mb-10" align="center" eyebrow={data.eyebrow} title={data.title} description={data.description} />
    <Grid class="gap-8 md:grid-cols-2 lg:grid-cols-3">
      {
        data.items.map((benefit, index) => (
          <article class="relative flex gap-5">
            <span class="inline-flex h-12 w-12 shrink-0 items-center justify-center rounded-full bg-[var(--color-primary)]/10 font-semibold text-[var(--color-primary)]">
              {String(index + 1).padStart(2, "0")}
            </span>
            <div>
              <h3 class="font-semibold text-[var(--color-text)]">{benefit.title}</h3>
              <p class="mt-2 text-sm leading-relaxed text-[var(--color-text-muted)]">{benefit.description}</p>
            </div>
          </article>
        ))
      }
    </Grid>
  </Container>
</Section>
