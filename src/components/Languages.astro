---
import Container from "./ui/Container.astro";
import Section from "./ui/Section.astro";
import Grid from "./ui/Grid.astro";
import Card from "./ui/Card.astro";
import SectionHeading from "./ui/SectionHeading.astro";

const languages = [
  {
    code: "BG",
    name: "Български",
    description: "Пълна подкрепа за всички бизнес нужди в България",
  },
  {
    code: "UA",
    name: "Українська",
    description: "Пълно консултантско обслужване за украински клиенти",
  },
  {
    code: "RU",
    name: "Русский",
    description: "Пълна подкрепа за рускоговорящи клиенти",
  },
];
---

<Section size="base" class="bg-gradient-to-br from-[var(--color-primary-dark)] to-[var(--color-primary)] text-[#fff7f3]">
  <Container>
    <Grid class="items-center gap-10 lg:grid-cols-2 lg:gap-12">
      <SectionHeading
        class="max-w-xl"
        eyebrow="Многоезична комуникация"
        title="Говорим вашия език"
        description="Комуникацията е ключова за качественото обслужване. Нашият екип предлага консултации и съдействие на три езика, така че нищо важно да не се губи в превода."
        invert={true}
      />

      <Grid class="gap-4 sm:grid-cols-3">
        {
          languages.map((language) => (
            <Card as="article" class="min-w-0 border-white/20 bg-white/10 p-6 backdrop-blur-sm">
              <p class="[font-family:'Playfair_Display',serif] text-3xl font-medium text-white">{language.code}</p>
              <h3 class="mt-1 text-lg font-medium text-white">{language.name}</h3>
              <p class="mt-2 text-sm leading-relaxed text-white/70">{language.description}</p>
            </Card>
          ))
        }
      </Grid>
    </Grid>
  </Container>
</Section>
